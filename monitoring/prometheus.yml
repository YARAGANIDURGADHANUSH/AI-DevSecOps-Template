global:
  scrape_interval: 15s

rule_files:
  - "/etc/prometheus/alerts/alert_rules.yml"

scrape_configs:

  - job_name: "ai-devsecops-template"

    static_configs:
      - targets:
          - "host.docker.internal:5000"