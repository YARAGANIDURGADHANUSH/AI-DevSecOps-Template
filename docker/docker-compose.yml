version: "3.9"

services:
  app:
    build:
      context: ..
      dockerfile: docker/Dockerfile

    container_name: ai-devsecops-template

    ports:
      - "5000:5000"

    restart: unless-stopped